# 검증 기준과 이력

> 현행 대상: v0.8.0 / 기준일: 2026-08-14
> 원칙: 확인 가능한 사실만 적고, 자격증명·제품 환경이 없어 못 한 시험은 `미확인`으로 남긴다.

## 1. 제출 목적

이 저장소는 소방 관련 민원에 GitHub 주소로 제안할 **소방기관 검토·시범운영용 구현체**다. “이미 소방청이
운영하는 공식 시스템”이나 “기관 운영 검증이 끝난 제품”으로 표현하지 않는다.

검토할 도입 방식은 두 가지다.

1. **기관 서버형**: 기관 서버가 웹/API/MCP 요청을 받고 정부 API를 조회한다. 선택한 LLM API를 연결하면
   조회 자료를 기반으로 답변한다.
2. **개별 PC 설치형**: 각 PC의 stdio MCP 지원 AI 클라이언트가 이 프로그램을 로컬로 실행하고 정부 API를
   조회한다.

두 방식 모두 소방청 공공데이터와 법제처 국가법령정보를 읽기 전용으로 조회한다. 법령 유권해석, 법률자문,
긴급 신고·출동, 현장 지휘, 소방특별조사를 대신하지 않는다.

## 2. 현행 기능 기준

- TypeScript MCP 서버이며 도구 11개를 `src/tool-registry.ts` 한 곳에 등록한다.
- 원천 데이터베이스를 배포하지 않고 HTTPS 정부 API와 최대 100항목 인메모리 LRU 캐시만 사용한다.
- 구급통계 범위는 전체 구급활동이 아닌 **교통사고 구급활동**이다.
- 웹/API 답변은 반드시 도구 조회를 먼저 한다. 조회 실패 시 자료 없는 LLM 답변을 만들지 않는다.
- 챗봇 질문 라우팅은 LLM 전용이다. LLM 미설정 시 HTTP 챗봇은 503으로 비활성화되고 `/mcp`·stdio는
  동작한다. 라우팅 실패 시 추측 조회를 하지 않는다.
- LLM 답변은 조회 자료 안의 내용만으로 작성하고 문장마다 `[자료 N]` 인용을 요구한다. 실존 자료 번호
  인용이 없으면 답변을 폐기하고 조회 원문을 표시하며, 공식 조회 자료는 항상 병기한다.
- HTTP 공개 운영 시 토큰, HTTPS, 접근통제, 프록시 로그 통제가 필요하다.
- PC 설치형 설정에는 정부 API 키가 평문으로 들어가므로 기관 관리 PC와 승인된 클라이언트만 대상으로 한다.
- 모델명·제품 UI·요금·API 한도는 변할 수 있으므로 운영 시점의 공식 문서를 다시 확인한다.

## 3. v0.6.0 전수감사에서 고정한 의도

수정 전에 기대 동작을 테스트로 추가해 실패를 확인하고, 구현 뒤 초록으로 전환했다.

| 발견한 빨간불 | 고정한 동작 |
|---|---|
| `/api/chat`이 요청별 정부 API 키를 실제 조회 컨텍스트에 전달하지 않음 | `X-Data-Go-Kr-Key`, `X-Law-Oc`를 MCP와 chat 모두 격리 전달 |
| 잘못된 LLM 공급자나 선택 공급자의 키 누락을 조회 모드로 숨김 | 시작 설정 오류로 즉시 거부 |
| OpenAI 레거시 Chat Completions·종료 예정 Gemini 기본 모델 | OpenAI Responses API와 현행 기본 모델로 교체 |
| 모든 지역에 `소방재난본부` 접미사를 붙임 | 인천·대구·제주 등 실제 기관명을 명시적으로 매핑 |
| 광주·전남 조직 통합 전후를 구분하지 않음 | 조회월 202607 전후로 과거/통합 본부명 분리 |
| 날짜가 있는 “구급 출동”을 화재통계로 분류 | 구급 의도를 우선하고 화재 날짜 분기는 화재 표현에만 적용 |
| HTTP 4xx/5xx XML 본문을 정상 데이터로 받을 가능성 | OpenAPI 오류 래퍼 확인 후 HTTP 상태도 필수 검사 |
| 서버 현지 시간대에 따라 이번 달 캐시 판단이 달라짐 | `Asia/Seoul` 기준 날짜로 통일 |
| 챗봇 오류·서버 로그에 대소문자 변형된 키 쿼리 노출 가능 | URL 민감값 마스킹을 대소문자 무관 적용 |
| 브라우저 저장값 내부 손상을 신뢰 | 대화·메시지 구조와 개수를 정규화하고 현재 탭 sessionStorage만 사용 |
| HTTP 청크가 한글 UTF-8 바이트 중간에서 나뉘면 질문이 `���`로 손상 | `StringDecoder`로 청크 경계를 보존 |
| 공개 HTTP 응답의 브라우저 보안 헤더 부재 | CSP, frame 차단, no-sniff, no-store 등 기본 헤더 적용 |
| Bearer 토큰을 일반 문자열 비교 | SHA-256 고정 길이 digest를 `timingSafeEqual`로 비교 |
| 지원 종료 Node와 오래된 의존성·문서 | Node 22 이상/Node 24 이미지, 현행 의존성·문서·CI로 갱신 |
| 라이선스·로컬 설치서·자동 CI 부재 | MIT LICENSE, `LOCAL_SETUP.md`, Node 22/24 GitHub Actions 추가 |
| HTTP 테스트가 고정 포트를 써 병렬 Node 검증끼리 충돌 | OS가 배정한 임시 포트와 서버 종료 대기 사용 |
| 대상물 API의 페이지별 `totalCount`를 전체건수로 오인 | 짧은 페이지까지 순회하고 실제 조회 건수만 표시 |
| 시행령·시행규칙 꼬리와 `10조`, `UN 1090`을 자연어에서 놓침 | 꼬리·조번호·UN/CAS 변형을 회귀 테스트로 고정 |
| 무효 날짜가 엉뚱한 법령 검색으로 폴백 | 조회 전에 달력 날짜를 검증하고 명확히 거부 |
| 행정규칙 검색이 제목만 제공하고 시행령 별표를 못 읽음 | 별표 원문과 NFPC/NFTC 행정규칙 원문 도구 추가 |
| 아파트 스프링클러 질문이 단일 검색 결과만 반환 | 시행령 별표 4·NFPC 103·NFTC 103을 함께 조회 |
| UI가 대화 문맥을 서버로 보내지 않음 | 최근 8개 메시지를 제한·정규화해 라우팅과 근거 선별에 전달 |
| 프롬프트만으로 LLM 환각을 막는다고 가정 | 원문과 정확히 일치하는 evidence만 허용하고 나머지는 폐기 |
| HTTP가 모든 인터페이스에 열리고 CLI 입력이 argv/사용자 작업공간에 노출 | loopback 기본·외부 인증 강제, stdin·임시 cwd·세션 비저장·동시성 제한 |

그 전 감사에서 이미 고정한 회귀 기준도 유지한다. 여기에는 키 이중 인코딩 방지, 키 URL 마스킹,
법제처 일시 404 재시도, 빈 결과 장기 캐시 금지, 한국 날짜, 건물 전체 페이지 필터, 요청 크기 제한,
IP rate limit 저장소 상한, 조회 없는 LLM 답변 차단, 검증된 근거와 원문 병기가 포함된다.

## 3-1. v0.7.0 라우팅 전환 (2026-08-14)

키워드 규칙 라우터(query-router.ts)는 실사용에서 오분류가 반복되어 수리 불가능하다고 판단, 삭제했다.
LLM 라우터가 유일한 라우터이며 다음을 고정한다.

- HTTP 챗봇(`GET /`, `/api/chat`)은 LLM 필수. 미설정 시 503과 설정 안내를 반환하고 `/mcp`·stdio는
  영향 없다.
- LLM 라우팅 실패 시 추측 조회로 폴백하지 않고 안내 오류를 반환한다 — 틀린 자료보다 명확한 실패가 낫다.
- 규칙 라우터가 갖고 있던 구급통계 기관명 매핑(광주·전남 202607 통합 분기 포함)은 llm-router 프롬프트의
  도메인 지식으로 이전해 보존했다.
- 유지: 날짜 달력 검증, 아파트 스프링클러 질문의 별표 4·NFPC 103·NFTC 103 동시 조회, 근거의 정확한
  부분문자열 검증, 근거 선별 실패 시 조회 원문 폴백.
- 2026-08-14 `npm run verify`: 타입검사·22개 테스트 파일 **165/165 통과**·배포 빌드 통과. 규칙 라우터
  테스트 25건이 삭제되어 총 테스트 수가 v0.6.0의 189건에서 줄었다.
- LLM 실호출 라우팅 정확도는 API 키·CLI 환경에서의 실측이 필요하다 — **미확인** (6장 승인 차단 조건에 포함).

## 3-2. v0.8.0 계획·답변 전환 (2026-08-14)

v0.7.0에 남아 있던 과잉 방어를 사용자 결정으로 제거했다. "조회 강제"는 유지하고 LLM에게 계획과 답변
역할을 돌려준다.

- **아파트 스프링클러 하드코딩 경로(installationRoutes) 삭제** — LLM 계획기가 별표·NFPC·NFTC 복수
  조회 필요성을 프롬프트 도메인 지식으로 스스로 판단한다. 동작 보존은 배포 전 실질문(§10 표)으로 확인한다.
- **단일 도구 제한 → 1~4개 호출 계획** — 각 호출을 Zod 스키마로 개별 검증하고, 유효 호출이 0개면
  추측 조회 없이 오류를 반환한다. 조회는 병렬 실행한다.
- **부분문자열 발췌기(GROUNDING_SYSTEM·verifiedEvidence) 삭제** — LLM이 조회 자료 안의 내용만으로
  자연어 답변을 작성하고 문장마다 `[자료 N]`을 인용한다. 서버는 모든 문장에 실존 자료 번호가 있는지
  기계 검증하고 공식 조회 자료를 병기한다. **부분문자열 게이트가 주던 문장 단위 원문 일치 보장은 사라진다 —
  의도된 트레이드오프로 기록한다** (검증 수단은 병기된 원문).
- 복수 조회 자료는 전체 길이를 끝에서 자르지 않고 자료별 길이 예산을 나눠 모든 자료 번호와 본문을 전달한다.
- 일부 조회 실패 시 성공분만으로 답하고 실패 도구를 답변에 표시한다. 전부 실패 시 답변을 생성하지 않는다.
- 질문 1건당 LLM 호출은 계획 1회 + 답변 1회로 유지된다.
- 2026-08-14 `npm run verify`: 타입검사·22개 테스트 파일 **172/172 통과**·배포 빌드 통과.
- LLM 실호출에서의 계획 품질(복수 조회 판단 포함)과 답변의 자료 준수는 API 키·CLI 환경 실측이 필요하다
  — **미확인** (6장 승인 차단 조건에 포함).

## 3-3. 본문검색 재정렬 + 인용 검증 완화 (2026-08-14)

`claude-cli`(claude-sonnet-5) 실측에서 발견한 병목 2건을 수정했다.

**병목 실측 (수정 전, "방화문" 질의)**

- 행정규칙 본문검색 폴백: 법제처 API에 관련도 정렬이 없어(실측 — `sort`는 발령일자 등 메타데이터
  정렬만 동작) 가나다순 상위 20건만 표시. "방화문" 본문 매칭 총 83건 중 소방청·국립소방연구원 소관
  NFPC·NFTC 약 20건이 ㅅ~ㅎ 구간이라 전부 절단되고, 경찰청 유치장 설계 규칙 등 무관 규정이 앞을 차지.
- 문장 단위 인용 전수 검증(`citedAnswer`): 실모델이 모든 문장에 `[자료 N]`을 달지 못해 답변이
  전량 폐기되고 항상 원문 덤프로 폴백 (`mode: lookup`). 로그 근거:
  `LLM 답변에 유효한 [자료 N] 인용이 없습니다.` 반복.

**수정 내용**

- `src/lib/relevance.ts` 신설: 본문검색 폴백 시 `display=100`으로 전량 수신 후 로컬 재정렬
  (제목 질의어 포함 +2, 우선 소관부처 +1, 동점은 가나다 유지). `search_fire_law`는
  소방청·국토교통부, `search_fire_admin_rules`는 소방청·국립소방연구원 우선. 무관 규정은
  버리지 않고 뒤로 보내며, 머리말에 `총 N건 중 관련도순 N건`으로 절단을 명시한다.
- 계획기 도메인 지식 추가: 방화문·방화구획·내화구조 등 건축물 구조·피난 기준의 본체는 국토교통부
  건축 법령(피난·방화구조 규칙 등)임을 명시, 애매하면 법령·행정규칙 둘 다 계획.
  `search_fire_admin_rules` 스키마 예시에서 오유도 원인이던 "방화문"을 제거.
- `citedAnswer` 완화: 실존 자료 번호 인용 ≥1 && 비실존 번호 인용 0 이면 통과 (문장 전수 검증 폐기).

**검증 (2026-08-14)**

- `npm run verify`: 타입검사·22개 테스트 파일 **173/173 통과**·배포 빌드 통과 (재정렬·부분 인용
  채택 테스트 추가, "일부 문장 인용 → 폐기" 테스트는 새 설계로 반전).
- `claude-cli` 실질의 "방화문": 계획기가 법령("건축물의 피난ㆍ방화구조 등의 기준에 관한 규칙" 1건
  정확 매칭)과 행정규칙을 함께 조회, 행정규칙 목록 상위 20건 전원 소방청·국립소방연구원 소관,
  답변 `mode: llm`으로 인용 포함 생성 확인. 3-2의 "LLM 실호출 계획 품질 미확인" 중 CLI 경로는
  이 실측으로 일부 해소 (API 키 공급자 3종은 여전히 미확인).

## 3-4. 원문 추출 구조 보강 (2026-08-14)

3-3 이후 실사용형 질문("아파트 계단실 방화문 기준, 60분 써야 하나") 실측에서 남은 병목:
계획기가 문서(피난·방화구조 규칙)는 특정하지만 문서 안의 관련 조문을 집지 못해 별표 4(수수료)를
조회하고 "확인 불가"로 답했다. 원문 도구 3종 중 행정규칙(`section`)·별표(`query`)는 키워드 추출이
있었으나 법령 조문(`get_fire_law_text`)만 없었다.

- `get_fire_law_text`에 `query` 추가 — 키워드가 등장하는 조문단위만 추출 (별표 도구와 동일 패턴).
- 계획기 일반 규칙 추가: 기준·수치·설치대상 내용 질문은 검색(목록)에 그치지 말고 원문 도구를 함께
  계획하고, 조·절 번호를 모르면 query/section 키워드 필터를 쓴다.
- 검증: `npm run verify` **176/176 통과** (조문 키워드 추출 2건, 법령 본문검색 재정렬 1건 추가).
  `claude-cli` 실질의 재실행 — 제9조를 정확히 추출해 피난계단 60분+/60분, 특별피난계단
  부속실→계단실 구간 30분 허용까지 조문 인용으로 답변, 자료에 없는 층수 기준은 "확인 불가" 표시.

미해결로 남긴 것(의도): 인용 게이트는 3-3 완화 상태 유지(위험 문장 선별 검증은 후속 논의),
본문검색 재정렬은 가나다순 앞 100건 창 안에서만 동작(totalCnt>100이면 페이징 필요).

## 4. 자동·로컬 실행 검증

아래 명령을 깨끗한 의존성 설치 상태에서 실행한 결과를 최종 제출 전 다시 기록한다.

```bash
npm ci
npm run verify
npm audit
npm pack --dry-run
```

검사 범위:

- 타입검사, 전체 Vitest 회귀 테스트, 배포 JavaScript 빌드
- stdio 시작과 stdout 프로토콜 오염 여부
- HTTP `/health`, `/`, `/api/chat`, `/mcp` 기동 및 인증·오류 상태
- 저장소 버전·Node 요구사항·문서·도구 목록·필수 파일 일관성
- 패키지 취약점, 배포 tarball 포함 파일, 저장소 내 비밀값 패턴

2026-08-13 최종 로컬 감사 결과:

| 검사 | 결과 |
|---|---|
| 깨끗한 설치 | Node 26.0.0 / npm 11.12.1에서 `npm ci` 성공, 설치 감사 0건 |
| 전체 검증 | 23개 테스트 파일, **189/189 통과**, 타입검사·배포 빌드 통과 |
| 지원 런타임 교차검증 | Node 22.23.2와 24.19.0에서 각각 타입검사·**189/189 통과** |
| HTTP 자동 기동 | health·웹·보안 헤더·chat·MCP 11개 도구·401 인증 경로 통과 |
| stdio 실제 기동 | MCP client로 initialize·tools/list 왕복, v0.6.0·도구 11개 확인 |
| 의존성·패키지 | `npm audit` 0건, `npm pack --dry-run` 66개 항목·필수 운영문서 포함 |
| 저장소 정적 점검 | `git diff --check` 통과, 일반적인 공급자 비밀키 패턴 없음, 로컬 Markdown 링크 전부 유효 |
| 깨끗한 복제 상당 점검 | `.git`·캐시·`.env`를 제외한 새 임시 디렉터리에서 `npm ci`와 189/189·빌드 통과 |

`npm outdated`에 표시되는 `@types/node` 26은 운영 기준인 Node 24 타입을 유지하기 위해 올리지 않았다. Zod 4는
`zod-to-json-schema` 연동을 함께 바꾸는 별도 메이저 마이그레이션이므로, 취약점이 없는 Zod 3 최신 패치를
유지했다.

v0.6.x 감사본은 커밋 `9a80247`로 확정되었다. 이 v0.7.0 작업본(규칙 라우터 삭제·LLM 필수화)은 아직
커밋 전이며, 민원에는 최종 커밋·푸시 후 그 커밋 URL을 사용해야 한다.

## 5. 실데이터 검증 이력

2026-08-13에 당시 자격증명으로 아래 9개 도구의 실제 응답을 확인한 기록이 있다.

| 도구 | 당시 확인 내용 |
|---|---|
| `search_fire_stats` | `20250101` 조회, 화재 관서별 필드 반환 |
| `get_ems_stats` | 서울 본부 조회, 교통사고 출동·이송 필드 반환 |
| `search_fire_building` | 서울특별시 대상물 조회 |
| `get_building_facilities` | 서울 호텔 필터와 스프링클러 필드 조회 |
| `search_hazmat` | 아세톤 CAS·분자식·물성·대응 정보 조회 |
| `search_fire_law` | 소방시설법 약칭으로 본법·시행령·시행규칙 조회 |
| `get_fire_law_text` | 소방시설법 제10조 본문 조회 |
| `search_fire_precedents` | 소방 판례 결과 조회 |
| `search_fire_admin_rules` | 스프링클러 NFPC·NFTC 결과 조회 |

이 기록은 원천 API와 코드가 바뀐 뒤의 성공을 보증하지 않는다. 특히 v0.6.0은 기관명 매핑, HTTP 오류 처리,
모델과 런타임을 바꿨으므로 배포 후보 커밋에서 다시 실호출해야 한다.

이번 작업본에서 같은 날 다시 확인한 범위:

- `20250101` 화재 179건을 `조회 179건`으로 표시
- 서울 시설 2·3페이지에 있던 `호텔벤` 2건과 `하이모텔` 1건 검색 성공
- `화재예방법 시행령 5조`가 본법이 아닌 시행령 제5조를 반환
- 소방시설법 시행령 별표 4의 `공동주택` 원문 반환
- `NFPC 103`과 `NFTC 103`을 `103A`와 구분해 정확한 행정규칙 ID·원문 반환
- `소방시설 점검` 0건에서 `소방시설`로 확장해 판례 9건 반환, `UN 1090`은 아세톤 상세 반환

현행 기관명 확인 근거:

- [소방청 본부 조직 목록](https://www.nfa.go.kr/nfa/introduce/organizationidfo/organization)
- [공공데이터포털 구급통계 API](https://www.data.go.kr/data/15099428/openapi.do) — 요청 예시의
  `sidoHqOgidNm` 값 포함
- [전남광주통합특별시 설치 법령](https://law.go.kr/lsInfoP.do?ancYnChk=0&chrClsCd=010202&efYd=20260701&lsId=&lsiSeq=284111&urlMode=lsEfInfoR&viewCls=lsRvsDocInfoR)

## 5-1. 로컬 CLI 어댑터 (2026-08-13)

로컬 개발·시연 중 API 키 없이 답변 생성을 확인하기 위해 `claude-cli`와 `codex-cli` 어댑터를 둔다.
질문은 argv가 아닌 stdin으로 전달하고 개인 설정·작업공간에서 격리하며 동시 실행과 대기열을 제한한다.

| 항목 | 결과 |
|---|---|
| Claude 실제 답변 생성 | ✅ `claude-sonnet-5`, 시행령 제5조 조회→근거검증 종단간 성공 |
| Codex 실제 답변 생성 | ✅ `gpt-5.6-sol`, 조문·대화 문맥 후속질문 종단간 성공 |
| LLM 라우팅 | ✅ 규칙 라우터가 못 잡던 "소방시설 제10"을 제10조로 해석 |
| 응답 지연 | 약 20초 (API 방식보다 느림, 기본 타임아웃 30초로는 부족해 120초 권장) |
| 자동 감지 제외 | ✅ `LLM_PROVIDER`로 명시할 때만 선택된다 (키 방식 동작 불변) |
| 실행 파일 부재 처리 | ✅ 설치 확인 안내 오류 반환 (테스트로 고정) |
| 전체 회귀 | ✅ 189/189 통과, 타입검사·빌드 통과 |

운영 제약: CLI가 설치·로그인된 PC에서만 동작하고, 개인 구독을 다중 사용자가 공유하는 형태가 되므로
**기관 서버 배포에는 사용하지 않는다.** 검토·시연·개발 단계 전용이며 배포는 API 키 방식을 쓴다.

## 6. 현재 미확인과 승인 차단 조건

| 항목 | 상태 | 승인 전에 할 일 |
|---|---|---|
| 현행 11개 정부 API 도구 실호출 | **대표·오류 수정 경로 확인** | 기관 시험에서 전체 입력 조합·한도 재시험 |
| Gemini·Claude·OpenAI API 호출 | **미확인** — API 공급자 키 시험 안 함 | 승인 공급자 하나 이상에서 복수 호출 계획·자료 기반 답변·timeout·폴백 확인 |
| Docker 이미지 build/run/health | **미확인** — Docker CLI 없음 | 기관 CI 또는 시험 서버에서 이미지 검증 |
| ChatGPT/Claude 원격 MCP와 인증 | **미확인** — 제품 시험 환경 없음 | 도구 스캔·인증·호출·회수 종단간 시험 |
| 서명된 PC 원클릭 패키지 | **미제공** | 수동 JSON이 불가하면 별도 패키징·서명 |
| 광주·전남 통합 후 구급 API의 새 기관명 실응답 | **미확인** | 202607 이후 자료로 실제 응답 확인 |

이 항목이 남은 상태에서는 “기관 운영 검증 완료”, “ChatGPT에 URL만 넣으면 즉시 사용”, “모든 구급통계
지원”이라고 안내하지 않는다. 검토·시범운영 후보로만 제출한다.

## 7. 외부 제품 현행성 재확인 링크

- [Node.js 지원 버전](https://nodejs.org/en/about/previous-releases)
- [OpenAI API quickstart와 Responses API](https://developers.openai.com/api/docs/quickstart)
- [OpenAI 현재 모델](https://developers.openai.com/api/docs/models/all)
- [Gemini 모델 종료 일정](https://ai.google.dev/gemini-api/docs/deprecations)
- [Claude 모델 개요](https://platform.claude.com/docs/en/about-claude/models/overview)
- [ChatGPT 개발자 모드와 MCP 앱](https://help.openai.com/en/articles/12584461-developer-mode-and-full-mcp-connectors-in-chatgpt-beta)
- [Claude Desktop 로컬 MCP](https://support.claude.com/en/articles/10949351-getting-started-with-local-mcp-servers-on-claude-desktop)

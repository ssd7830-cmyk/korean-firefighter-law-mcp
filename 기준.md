# 기준.md — 확정된 의도 기록

> 확인 가능한 문장으로만 적는다. 완성 시 항목마다 증거를 붙여 보고하고, 증거 없으면 '미확인'.

## 확정 (2026-08-13)

1. **결과물은 MCP 서버다. 대시보드는 만들지 않는다.**
   - 검증: 저장소에 HTML/프런트엔드 코드가 없다. `npx` 실행 시 MCP stdio 서버가 뜬다.
2. **사용자는 실제 소방관계자다. 강의 시연용이 아니다.**
   - 검증: 도구 설명·에러 메시지가 실무 용어로 작성돼 있고, 데모용 목업 화면이 없다.
3. **데이터는 API 실시간 호출로 가져온다. DB를 저장소에 배포하지 않는다.**
   - 검증: 저장소에 .sqlite/.db 파일이 없다. 모든 조회가 data.go.kr 또는 law.go.kr HTTPS 호출로 나간다.
4. **캐시는 인메모리 LRU만 쓴다. SQLite는 호출 한도가 실측으로 문제 될 때만 추가한다.**
   - 검증: package.json에 DB 의존성이 없다. cache.ts는 CacheStore 인터페이스 + 메모리 구현뿐이다.
5. **구조 원칙: TypeScript, tool-registry 중앙 등록, tools→lib→client 단방향 의존.**
   - 검증: src/tool-registry.ts에 allTools[]가 있고 도구가 전부 여기 등록된다.
6. **인증키는 환경변수 2개** (`DATA_GO_KR_KEY`, `LAW_OC`). 로그인·세션 코드는 없다.
   - 검증: .env.example에 키 2개만 있다. 코드에 비밀번호/쿠키 처리 없음.

## 작업 규칙 (카파시 가이드라인)

- 요청 안 한 기능 만들지 않기
- 같은 결과면 짧은 쪽 고르기
- 다 만든 뒤 빼도 되는 코드 찾아서 빼기

## 추가 확정 (2026-08-13, 사용자 답변)

7. **소방관 전체 배포용 — 특정 업무 우선순위 없이 도구 7개 전부 만든다.** ("다만드셈 소방관들한테 뿌릴꺼임")

## 검증 보고 (v0.1 완성 시점)

| # | 기준 | 결과 | 증거 |
|---|---|---|---|
| 1 | HTML/프런트엔드 없음, MCP stdio 서버 | ✅ | 저장소에 .html 0개. `node build/index.js` 실행 시 stdio 서버 기동 로그 확인 |
| 2 | 실무용 (시연용 목업 없음) | ✅ | 목데이터·데모 화면 없음. 에러 메시지가 실제 발급 절차 안내 |
| 3 | .sqlite/.db 파일 없음, 전부 HTTPS 호출 | ✅ | DB 파일 0개. 호출은 apis.data.go.kr / law.go.kr 두 곳뿐 |
| 4 | DB 의존성 없음, CacheStore+메모리 구현만 | ✅ | package.json 의존성 4개(mcp-sdk, zod, zod-to-json-schema, fast-xml-parser) |
| 5 | tool-registry에 allTools[] 중앙 등록 | ✅ | tools/list 응답에 7개 도구 전부 노출 (스모크 테스트) |
| 6 | 키 2개만, 로그인 코드 없음 | ✅ | .env.example에 DATA_GO_KR_KEY, LAW_OC. 키 마스킹 테스트 통과 |
| — | 타입체크·빌드·단위테스트 | ✅ | tsc 에러 0, vitest 11/11 통과 |
| — | 키 없이 실행 가능 경로 | ✅ | search_fire_law(무인자) → 법령 목록 정상. 키 필요 도구 → 발급 안내 에러 |
| — | 실데이터 API 응답 (4개 소방청 API + 법제처) | **미확인** | 인증키 미발급 상태라 실호출 불가. 키 발급 후 검증 필요 |
| — | 특정소방대상물 2개 오퍼레이션명 | **미확인** | 활용가이드 문서(활용신청 후 다운로드)로 확정 필요. env로 교체 가능하게 해둠 |

## TDD 루프 기록 (2026-08-13, 의도 테스트로 빨간불 사냥)

테스트 11개 → **61개**. 의도를 테스트로 박고 빨간불을 잡는 루프를 사냥감이 마를 때까지 반복.

잡은 빨간불 (전부 수정 후 초록 확인):

1. **별칭 안 띄어쓰기 붕괴** — "화재 예방법 시행령" 입력 시 "…법률 방법 시행령"으로 오변환. 공백 제거본 기준으로 치환하도록 수정
2. **조번호 띄어쓰기 거부** — "제 10 조"가 에러. 공백 제거 후 해석하도록 수정
3. **필터 후 전체건수 왜곡** — 건물명 필터 후에도 원본 totalCount(수천 건)를 표시 → 필터 후 개수로 갱신하는 filterBodyByKeyword 신설
4. **통계 TTL 검증 불가** — statsTtl 미노출로 캐시 정책이 테스트 사각지대 → 공용 헬퍼로 승격
5. **빈 결과 7일 박제** — 원천 데이터 입력 지연 중 빈 응답이 확정통계 TTL로 캐시되어 일주일간 "결과 없음" 오답 → 빈 결과는 캐시 제외
6. **구급통계 이번 달 박제** — 이번 달·월 미지정 조회까지 7일 캐시 → 기간 확정 여부로 TTL 판정 (statsTtlFor 공용화)
7. **깨진 응답 원문 노출** — JSON도 XML도 아닌 200 응답에 날 파싱 에러 노출 → 재시도 안내 메시지로 전환

추가 확보한 회귀 가드: 인증키 인코딩/이중 인코딩 방지, 키 마스킹, Referer 주입 범위(law.go.kr만), DRF 404 재시도, 본법-시행령 정확 매칭 선택, 단일 결과 XML 처리, 50건 초과 생략 표기, 조·항·호·목 재귀 수집, 레지스트리 무결성(이름 중복·스키마 변환).

## v0.3 (HTTP 모드 + 인수인계 패키지) 검증 보고 (2026-08-13)

| 기준 | 결과 | 증거 |
|---|---|---|
| 받는 쪽이 배포·운영 전담 가능 | ✅ | DEPLOY.md에 Docker/fly.io/맨Node 3경로 + 키 발급 + 커넥터 등록 전부 문서화 |
| HTTP stateless MCP 서버 동작 | ✅ | 실서버 기동 후 curl로 /health=ok, tools/list 7개 JSON 응답 확인 |
| 사전 initialize 없는 독립 요청 처리 (stateless) | ✅ | 테스트: tools/list 단독 POST 성공 |
| 동시 요청 간 인증키 격리 | ✅ | 테스트: 겹치는 요청 2개가 각자 헤더 키 사용, 컨텍스트>env 우선순위 확인 |
| 공개 URL 남용 방지 | ✅ | SERVER_AUTH_TOKEN 설정 시 401, Bearer 일치 시 통과 테스트 |
| stdio 모드 무손상 | ✅ | 기존 61개 포함 전체 71/71 테스트 통과, 타입체크 0 에러 |
| Docker 이미지 빌드 | **미확인** | 이 컴퓨터에 Docker 데몬 없음. Dockerfile은 표준 구성이나 실빌드는 받는 쪽 확인 필요 |

## 계획과 달라진 것

- 구급통계는 스펙이 공개 확인된 "교통사고 구급활동" 오퍼레이션만 연결 (서비스에 오퍼레이션 더 있음 — 키 발급 후 확장)
- 체인 도구(check_building_compliance)는 설계대로 v0.2로 미룸

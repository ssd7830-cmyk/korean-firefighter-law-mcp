# 기준.md — 확정된 의도 기록

> 확인 가능한 문장으로만 적는다. 완성 시 항목마다 증거를 붙여 보고하고, 증거 없으면 '미확인'.

## 확정 (2026-08-13)

1. **결과물은 MCP 서버다. 대시보드는 만들지 않는다.**
   - 검증: 저장소에 HTML/프런트엔드 코드가 없다. `npx` 실행 시 MCP stdio 서버가 뜬다.
   - **개정 (v0.4)**: 소방관용 챗봇 페이지 1장(`src/web/chat-page.ts`, HTTP 모드 `GET /`)을 확정 범위에 추가. 대시보드·그 외 프런트엔드 금지는 유지.
2. **사용자는 실제 소방관계자다. 강의 시연용이 아니다.**
   - 검증: 도구 설명·에러 메시지가 실무 용어로 작성돼 있고, 데모용 목업 화면이 없다.
3. **데이터는 API 실시간 호출로 가져온다. DB를 저장소에 배포하지 않는다.**
   - 검증: 저장소에 .sqlite/.db 파일이 없다. 모든 조회가 data.go.kr 또는 law.go.kr HTTPS 호출로 나간다.
4. **캐시는 인메모리 LRU만 쓴다. SQLite는 호출 한도가 실측으로 문제 될 때만 추가한다.**
   - 검증: package.json에 DB 의존성이 없다. cache.ts는 CacheStore 인터페이스 + 메모리 구현뿐이다.
5. **구조 원칙: TypeScript, tool-registry 중앙 등록, tools→lib→client 단방향 의존.**
   - 검증: src/tool-registry.ts에 allTools[]가 있고 도구가 전부 여기 등록된다.
6. **인증키는 환경변수 2개** (`DATA_GO_KR_KEY`, `LAW_OC`). 로그인·세션 코드는 없다.
   - 검증: .env.example에 키 2개만 있다. 코드에 비밀번호/쿠키 처리 없음.
   - **개정 (v0.4)**: 챗봇 AI 답변용 LLM 키(`GEMINI_API_KEY`|`ANTHROPIC_API_KEY`|`OPENAI_API_KEY` 중 택1)는 선택 — 없으면 조회 모드로 동작.

## 작업 규칙 (카파시 가이드라인)

- 요청 안 한 기능 만들지 않기
- 같은 결과면 짧은 쪽 고르기
- 다 만든 뒤 빼도 되는 코드 찾아서 빼기

## 추가 확정 (2026-08-13, 사용자 답변)

7. **소방관 전체 배포용 — 특정 업무 우선순위 없이 도구 7개 전부 만든다.** ("다만드셈 소방관들한테 뿌릴꺼임")

## 검증 보고 (v0.1 완성 시점)

| # | 기준 | 결과 | 증거 |
|---|---|---|---|
| 1 | HTML/프런트엔드 없음, MCP stdio 서버 | ✅ | 저장소에 .html 0개. `node build/index.js` 실행 시 stdio 서버 기동 로그 확인 |
| 2 | 실무용 (시연용 목업 없음) | ✅ | 목데이터·데모 화면 없음. 에러 메시지가 실제 발급 절차 안내 |
| 3 | .sqlite/.db 파일 없음, 전부 HTTPS 호출 | ✅ | DB 파일 0개. 호출은 apis.data.go.kr / law.go.kr 두 곳뿐 |
| 4 | DB 의존성 없음, CacheStore+메모리 구현만 | ✅ | package.json 의존성 4개(mcp-sdk, zod, zod-to-json-schema, fast-xml-parser) |
| 5 | tool-registry에 allTools[] 중앙 등록 | ✅ | tools/list 응답에 7개 도구 전부 노출 (스모크 테스트) |
| 6 | 키 2개만, 로그인 코드 없음 | ✅ | .env.example에 DATA_GO_KR_KEY, LAW_OC. 키 마스킹 테스트 통과 |
| — | 타입체크·빌드·단위테스트 | ✅ | tsc 에러 0, vitest 11/11 통과 |
| — | 키 없이 실행 가능 경로 | ✅ | search_fire_law(무인자) → 법령 목록 정상. 키 필요 도구 → 발급 안내 에러 |
| — | 실데이터 API 응답 (4개 소방청 API + 법제처) | **미확인** | 인증키 미발급 상태라 실호출 불가. 키 발급 후 검증 필요 |
| — | 특정소방대상물 2개 오퍼레이션명 | **미확인** | 활용가이드 문서(활용신청 후 다운로드)로 확정 필요. env로 교체 가능하게 해둠 |

## TDD 루프 기록 (2026-08-13, 의도 테스트로 빨간불 사냥)

테스트 11개 → **61개**. 의도를 테스트로 박고 빨간불을 잡는 루프를 사냥감이 마를 때까지 반복.

잡은 빨간불 (전부 수정 후 초록 확인):

1. **별칭 안 띄어쓰기 붕괴** — "화재 예방법 시행령" 입력 시 "…법률 방법 시행령"으로 오변환. 공백 제거본 기준으로 치환하도록 수정
2. **조번호 띄어쓰기 거부** — "제 10 조"가 에러. 공백 제거 후 해석하도록 수정
3. **필터 후 전체건수 왜곡** — 건물명 필터 후에도 원본 totalCount(수천 건)를 표시 → 필터 후 개수로 갱신하는 filterBodyByKeyword 신설
4. **통계 TTL 검증 불가** — statsTtl 미노출로 캐시 정책이 테스트 사각지대 → 공용 헬퍼로 승격
5. **빈 결과 7일 박제** — 원천 데이터 입력 지연 중 빈 응답이 확정통계 TTL로 캐시되어 일주일간 "결과 없음" 오답 → 빈 결과는 캐시 제외
6. **구급통계 이번 달 박제** — 이번 달·월 미지정 조회까지 7일 캐시 → 기간 확정 여부로 TTL 판정 (statsTtlFor 공용화)
7. **깨진 응답 원문 노출** — JSON도 XML도 아닌 200 응답에 날 파싱 에러 노출 → 재시도 안내 메시지로 전환

추가 확보한 회귀 가드: 인증키 인코딩/이중 인코딩 방지, 키 마스킹, Referer 주입 범위(law.go.kr만), DRF 404 재시도, 본법-시행령 정확 매칭 선택, 단일 결과 XML 처리, 50건 초과 생략 표기, 조·항·호·목 재귀 수집, 레지스트리 무결성(이름 중복·스키마 변환).

## v0.3 (HTTP 모드 + 인수인계 패키지) 검증 보고 (2026-08-13)

| 기준 | 결과 | 증거 |
|---|---|---|
| 받는 쪽이 배포·운영 전담 가능 | ✅ | DEPLOY.md에 Docker/fly.io/맨Node 3경로 + 키 발급 + 커넥터 등록 전부 문서화 |
| HTTP stateless MCP 서버 동작 | ✅ | 실서버 기동 후 curl로 /health=ok, tools/list 7개 JSON 응답 확인 |
| 사전 initialize 없는 독립 요청 처리 (stateless) | ✅ | 테스트: tools/list 단독 POST 성공 |
| 동시 요청 간 인증키 격리 | ✅ | 테스트: 겹치는 요청 2개가 각자 헤더 키 사용, 컨텍스트>env 우선순위 확인 |
| 공개 URL 남용 방지 | ✅ | SERVER_AUTH_TOKEN 설정 시 401, Bearer 일치 시 통과 테스트 |
| stdio 모드 무손상 | ✅ | 기존 61개 포함 전체 71/71 테스트 통과, 타입체크 0 에러 |
| Docker 이미지 빌드 | **미확인** | 이 컴퓨터에 Docker 데몬 없음. Dockerfile은 표준 구성이나 실빌드는 받는 쪽 확인 필요 |

## v0.4 (챗봇 사이트 + LLM 어댑터) 검증 보고 (2026-08-13)

| 기준 | 결과 | 증거 |
|---|---|---|
| ChatGPT식 화면 (사이드바·채팅창·입력창) | ✅ | GET / 가 HTML 반환, 실서버 curl로 title 확인. 대화목록은 localStorage |
| 할루시네이션 차단: 질문 → 무조건 조회 → 자료 내 답변 | ✅ | 파이프라인 테스트: 조회 결과가 LLM 자료로 전달됨 확인, 조회 실패 시 LLM 미호출 확인, 자료 없는 답변 경로 없음 |
| 라우터가 질문을 올바른 도구로 보냄 | ✅ | 테스트 8케이스: 조문·판례·화재날짜·구급시도·건물·기본값. 실서버에서 "소방시설법 제10조" → get_fire_law_text 라우팅 확인 |
| LLM 3사 어댑터 (키만 꽂으면 동작) | ✅ | 어댑터 선택·자동감지·우선순위 테스트. 제미나이 응답 파싱 테스트. Claude는 공식 SDK 사용 |
| LLM 키 없이도 사이트 동작 (조회 모드) | ✅ | 키 전무 상태 실서버에서 answer/mode/tool JSON 정상 반환 |
| LLM 실패 시 답변 증발 금지 | ✅ | 테스트: 생성 실패 시 조회 결과 원문으로 폴백 |
| 기존 기능 무손상 | ✅ | 전체 91/91 테스트 통과, 타입체크 0 에러 |
| 실제 LLM API 호출 (3사) | **미확인** | LLM 키 미보유. 받는 쪽이 키 꽂은 뒤 첫 질문으로 확인 |

## 실데이터 검증 — 법제처 (2026-08-13, LAW_OC 발급 후)

| 도구 | 결과 | 증거 |
|---|---|---|
| `search_fire_law` | ✅ | "소방시설법" → 약칭 해석 후 본법·시행령·시행규칙 3건, MST 포함 |
| `get_fire_law_text` | ✅ | `lawName:"소방시설법", jo:"제10조"` → 제10조(주택에 설치하는 소방시설) ①②③ 정확 반환 |
| `search_fire_precedents` | ✅ | "소방" → 판례 20건 (판례ID·법원·선고일 포함). 단 "소방시설 화재"·"소방 관련" 등 복합 키워드는 0건 |
| 챗봇 `/api/chat` (조회 모드) | ✅ | "소방시설법 제10조 보여줘" → 라우팅·조문 반환 정상 |
| 소방청 4개 API (`DATA_GO_KR_KEY`) | **미확인** | 키 미발급 — data.go.kr 활용신청 대기 |

발견된 개선 후보 2건 → **수정 완료** (2026-08-13):
1. 챗봇 답변 헤더 "…법률 10" 표기 → `joLabel()` 신설(search-normalizer), 헤더가 입력 형식과 무관하게 "제N조(의M)"로 통일. 실서버 재검증: "…법률 제10조" 확인
2. 판례 복합 키워드 0건 → 라우터 cleanQuery에 군더더기 제거(관련·에 대해) + 공백 정리 추가. 실서버 재검증: "소방 관련 판례 찾아줘" → 20건
- 회귀 가드: 테스트 3개 추가 (joLabel 2, 라우터 1) — 전체 94/94 통과, 타입체크 0 에러

## LLM 라우팅 추가 (2026-08-13, "개떡같이 말해도 찰떡같이" 요청)

- 확정: LLM 키가 있으면 질문 해석(어느 도구, 어떤 인자)을 LLM이 담당. 도구 7개의 스키마(레지스트리에서 자동 생성)와 법령 약칭 사전을 프롬프트로 주입 — 별도 온톨로지 파일 없이 기존 코드가 단일 출처
- 안전장치: LLM이 없는 도구를 지어내거나 인자가 스키마에 안 맞으면 즉시 규칙 라우터로 폴백. LLM은 도구 선택만 하고 답변 자료는 여전히 API 조회에서만 나옴 (할루시네이션 차단 유지)
- 검증: 라우터 단위 테스트 6개 + 파이프라인 통합 1개, 전체 101/101 통과. LLM 키 없는 실서버가 종전과 동일 동작(규칙 경로)인 것 확인
- **미확인**: 실제 LLM의 라우팅 품질 — 키 발급 후 "소방시설 제10" 같은 변형 질문으로 확인 필요

## 내용 질문 대응 (2026-08-13, "방화문 설치 기준이 안 나온다" 신고)

- 원인: ① search_fire_law가 법령 "이름"으로만 검색 ② 소방시설 설치 기준의 본체인 화재안전기준(NFPC/NFTC)은 행정규칙인데 검색 도구가 없었음
- 수정: ① 법령·행정규칙 검색 모두 이름 0건이면 본문검색(search=2) 자동 폴백 ② `search_fire_admin_rules` 도구 추가 (7→8개) + 라우터에 화재안전기준·고시 분기
- 검증(실서버): "방화문 설치 기준" → 본문검색 20건(건축물의 피난ㆍ방화구조 규칙 등) / "스프링클러 화재안전기준" → NFPC·NFTC 6건. 전체 테스트 105/105, 타입체크 0 에러

## 실데이터 검증 — 소방청 공공데이터 (2026-08-13, DATA_GO_KR_KEY 발급 후)

활용가이드 문서(포털에서 다운로드)와 실호출로 확정한 실스펙:
- 1661000 계열 공통: `resultType` 파라미터 필수 (없으면 INVALID PARAMETER, json 모드는 빈 결과 → xml 강제)
- 건물: `SpecificFireObjectInfoService/getAccomList`, 시설: `SpecificFireObjectFirefightingSysInfoService/getAccomFirefightingSysList` — 설계 시 추정값과 달라 교체
- 위험물(`materialInfoSvc`): 응답 변형 3종(response 래퍼 없음·items 배열·상세는 body.item 단수, resultCode "0") → 클라이언트에서 통일
- 응답 필드 한글 라벨은 가이드 문서 국문명 기준으로만 표기 (추측 금지 원칙)

| 도구 | 결과 | 증거 (실서버) |
|---|---|---|
| search_fire_stats | ✅ | 20250101 → 179건, 소방서별 접수·진행·오보 (한글 라벨) |
| get_ems_stats | ✅ | 서울소방재난본부 → 15,592건 (출동·이송환자) |
| search_fire_building | ✅ | 서울특별시 → 3,402건 (대상물명·주용도·소방서) |
| get_building_facilities | ✅ | 서울 "호텔" 필터 → 10건, 스프링클러 (H)/(AV) 설치여부 |
| search_hazmat (신규 9번째) | ✅ | "아세톤 위험물이야?" → CAS 67-64-1·CH3COCH3·끓는점 56℃·유출 대응 |

- 위험물 도구 추가 확정 — 사용자가 국가위험물정보 API를 직접 활용신청해 승인받음. 목록은 서버 필터가 없어 전체 7,277건을 1회 호출로 받아 24h 캐시 후 클라이언트 매칭
- 전체 테스트 112/112, 타입체크 0 에러. 이로써 도구 9개 전부 실데이터 검증 완료 — 남은 미확인은 LLM 실호출(키 미보유)뿐

## 계획과 달라진 것

- 구급통계는 스펙이 공개 확인된 "교통사고 구급활동" 오퍼레이션만 연결 (서비스에 오퍼레이션 더 있음 — 키 발급 후 확장)
- 체인 도구(check_building_compliance)는 설계대로 v0.2로 미룸
- "프런트엔드 없음" 확정(항목 1)을 v0.4에서 개정 — 소방관 배포용 챗봇 페이지 1장 추가, 대시보드 금지는 유지

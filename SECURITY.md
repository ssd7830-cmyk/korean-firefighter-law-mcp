# 보안 정책

## 지원 범위

기관 검토는 `main`의 최신 릴리스 또는 기관이 고정한 커밋을 대상으로 합니다. 과거 커밋은 별도 지원을
보장하지 않습니다.

## 취약점·키 노출 신고

API 키, 토큰, 개인정보를 공개 Issue에 올리지 마세요. GitHub의 비공개 취약점 신고 기능이 활성화되어 있으면
그 경로를 사용하고, 사용할 수 없으면 저장소 소유자와 기관의 승인된 비공개 연락 경로로 다음만 전달합니다.

- 영향받는 커밋과 실행 방식(stdio/HTTP)
- 재현 절차와 예상 영향
- 비밀값을 제거한 로그 또는 최소 재현

실제 키나 토큰이 노출되었다면 수정 배포를 기다리지 말고 먼저 발급처에서 폐기·재발급하고, 프록시와
호스팅 로그의 노출 범위도 확인합니다.

## 운영 기본선

- `/mcp`, `/api/chat`은 HTTPS와 인증 없이 공용 인터넷에 노출하지 않습니다.
- HTTP 서버의 기본 바인딩은 `127.0.0.1`이며 외부 `HOST`는 접속 토큰 없이는 시작되지 않습니다.
- 기관 비밀 저장소를 사용하고 `.env`·클라이언트 설정 파일을 Git에 커밋하지 않습니다.
- `TRUST_PROXY=true`는 신뢰 프록시가 외부 `X-Forwarded-For`를 제거한 뒤 다시 설정할 때만 사용합니다.
- LLM 사용 전 질문과 조회 자료의 외부 전송·보존·국외이전 정책을 검토합니다.
- 로컬 Claude/Codex CLI 어댑터는 질문을 stdin으로 전달하고 작업공간 밖에서 세션 비저장·도구 제한 또는
  read-only로 실행하지만, 개인 로그인을 다중 사용자 기관 서버에 공유하는 운영 방식으로 쓰지 않습니다.
- 이 저장소는 사용자별 권한관리, 감사로그, OAuth/OIDC를 자체 제공하지 않습니다. 필요하면 기관
  게이트웨이에서 구현합니다.

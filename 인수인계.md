# 소방 AI 도우미 인수인계서

> 적용 버전: v0.6.0 / 기준일: 2026-08-13
>
> 이 문서는 기관이 GitHub 저장소를 인수해 검토·시범운영·유지보수할 때 필요한 책임과 절차를 정리합니다.
> 외부 서비스의 모델·가격·승인·트래픽·제품 UI는 바뀌므로 운영 시점의 공식 문서를 우선합니다.

## 1. 목적과 비목적

소방관계자가 자연어로 질문하면 소방청 공공데이터와 법제처 국가법령정보를 조회하는 읽기 전용 도구입니다.
기관은 다음 두 방식 중 하나 또는 둘 다 검토할 수 있습니다.

1. **기관 서버형**: 기관 서버의 웹/API가 질문을 받고, 선택한 LLM API가 조회 자료를 요약합니다.
2. **개별 PC 설치형**: 각 기관 PC의 stdio 지원 AI 클라이언트가 로컬 프로그램을 실행합니다.

이 프로그램은 소방청·법제처의 공식 서비스가 아니며 다음을 대신하지 않습니다.

- 법령의 유권해석, 법률자문, 소방특별조사 또는 현장 지휘
- 긴급 신고·출동 시스템
- 전체 구급통계 시스템. 현재 구급 도구는 **교통사고 구급활동**만 조회
- 개인정보·민감정보를 처리하도록 승인된 업무시스템

## 2. 핵심 안전 구조

```text
질문
  ↓
도구와 인자 결정(LLM 또는 규칙)
  ↓
정부 공식 API 조회 ── 실패하면 오류를 반환하고 답변 생성 중단
  ↓
LLM 없음: 조회 원문 반환
LLM 있음: 조회 자료만 요약 + 공식 조회 원문 병기
```

조회 자료 없이 LLM이 자유 답변을 만드는 경로는 두지 않았습니다. 다만 원천 API의 오류·지연과 LLM의 요약
오류는 여전히 가능하므로 중요한 판단은 표시된 공식 원문과 원천 시스템에서 다시 확인해야 합니다.

## 3. 기능과 데이터 출처

| 구분 | 기능 | 출처 |
|---|---|---|
| 화재 | 날짜별 관서 단위 접수·진행·오인·자체진화 현황 | 소방청 화재정보서비스 |
| 구급 | 시도·연월별 교통사고 출동·이송 통계 | 소방청 구급통계서비스 |
| 대상물 | 시도·건물명·사용승인연도별 검색 | 소방청 특정소방대상물정보 |
| 시설 | 특정소방대상물의 소방시설 현황 | 소방청 소방시설정보 |
| 법령 | 소방 관계 법령 이름·본문 검색, 조문 조회 | 법제처 국가법령정보 |
| 판례 | 소방 관련 판례 검색 | 법제처 국가법령정보 |
| 행정규칙 | 고시·훈령, NFPC·NFTC 이름·본문 검색 | 법제처 국가법령정보 |
| 위험물 | 물질명·CAS·UN번호별 위험물 정보 | 소방청 국가위험물정보 |

도구명과 정확한 입력 범위는 [README.md](README.md), 내부 구조는 [ARCHITECTURE.md](ARCHITECTURE.md)를
참고합니다.

## 4. 도입 방식 선택

| 항목 | 기관 서버형 | 개별 PC 설치형 |
|---|---|---|
| 사용자 | 브라우저 또는 기관 서비스/API | stdio 지원 AI 클라이언트 사용자 |
| 설치 | 서버와 앞단 프록시 | 허용된 모든 PC |
| 정부 API 키 | 서버에 중앙 보관 | 각 PC에 설정 또는 기관 배포 |
| LLM | 서버가 Gemini/Claude/OpenAI API 중 선택 | AI 클라이언트가 제공 |
| 업데이트 | 서버 배포 한 번 | 표준 이미지/배포도구로 전 PC 갱신 |
| 장점 | 사용자 설치 없음, 중앙 통제 | 중앙 서비스 없이 동작, 키·처리가 PC 단위 |
| 주요 위험 | 외부 공개·공유 토큰·LLM 전송·서버 운영 | 평문 설정 키·PC별 편차·회수와 업데이트 |

기관 서버형 절차는 [DEPLOY.md](DEPLOY.md), PC 설치형 절차는 [LOCAL_SETUP.md](LOCAL_SETUP.md)를 따릅니다.
ChatGPT는 로컬 MCP 서버에 직접 연결하지 않으므로 개별 PC 설치형을 “ChatGPT 설치 방식”으로 안내하면 안 됩니다.

## 5. 도입 전 승인 체크리스트

- [ ] 시스템 소유부서·운영부서·정보보호 담당자 지정
- [ ] 공공데이터포털·법제처 계정을 기관 명의로 발급하고 키 보관·회수 담당 지정
- [ ] 원천 데이터 이용조건과 기관 내부 재이용 정책 검토
- [ ] 외부 LLM 사용 여부와 질문·조회 자료의 외부 전송, 보존, 국외이전, 비용 검토
- [ ] 개인정보·민감정보 입력 금지 또는 승인된 처리 기준 마련
- [ ] 이용자 인증·권한, 로그 보존, 장애·유출 대응 절차 마련
- [ ] 시범 사용자·기간·평가 질문·정확도 판정자 지정
- [ ] 결과가 참고자료이며 공식 판단을 대신하지 않는다는 사용자 고지

LLM 정책 승인이 없으면 LLM 키 없이 조회 모드로 먼저 시범운영할 수 있습니다.

## 6. 인증키와 비밀정보

필수 정부 키는 두 종류입니다.

| 변수 | 발급처 | 필요한 활용신청 |
|---|---|---|
| `DATA_GO_KR_KEY` | [공공데이터포털](https://www.data.go.kr) | README에 연결된 소방청 API 5개 |
| `LAW_OC` | [법제처 공동활용](https://open.law.go.kr) | 법령·판례·행정규칙 검색/본문 |

운영 규칙:

- `.env`와 AI 클라이언트 설정 파일은 Git에 올리지 않습니다.
- 기관 비밀관리 시스템 또는 OS 보안 저장소를 사용하고 최소 인원만 접근합니다.
- 채팅용 `CHAT_AUTH_TOKEN`과 원격 MCP용 `SERVER_AUTH_TOKEN`을 분리합니다.
- 키를 메신저·공개 이슈·스크린샷·민원 본문에 붙이지 않습니다.
- 유출 의심 시 발급처에서 회수·재발급하고 애플리케이션뿐 아니라 프록시·호스팅 로그를 조사합니다.

애플리케이션은 자체 오류의 키 값을 마스킹하지만 모든 외부 로그를 통제하지는 못합니다. 비암호화 HTTP로
법제처 키를 보내는 설정은 운영에서 사용하지 않습니다.

## 7. 기관 서버형 인수

### 필수 구성

- Docker가 가능한 서버 또는 Node.js 22 이상 실행환경
- TLS를 제공하는 기관 리버스프록시/로드밸런서
- 정부 API 키 2종과 긴 무작위 접속 토큰 2종
- 선택: 승인된 LLM API 계정
- health·오류율·자원 사용량을 보는 모니터링

### 인수자가 직접 수행할 명령

```bash
git clone https://github.com/ssd7830-cmyk/korean-firefighter-law-mcp.git
cd korean-firefighter-law-mcp
npm ci
npm run verify
```

그 다음 [DEPLOY.md](DEPLOY.md)의 Docker 절차로 시험 서버를 띄웁니다. 공개 주소는 코드의 8080 포트에
직접 연결하지 말고 TLS와 기관 인증/접근통제가 있는 앞단을 통과시킵니다.

### 원격 MCP 주의

`/mcp`가 존재한다고 ChatGPT·Claude에 URL만 넣어 바로 운영할 수 있다는 뜻은 아닙니다. 제품별 요금제,
관리자 권한, 인증 요구사항을 확인하고 다음을 종단간 시험합니다.

- 도구 9개 스캔
- 인증 성공·실패·만료와 권한 회수
- 실제 도구 호출과 긴 결과 처리
- 도구 변경 후 관리자 재검토/새로고침

현재 단순 Bearer 토큰의 각 제품 UI 호환은 이 개발 환경에서 검증하지 못했습니다. 필요하면 기관
OAuth/OIDC 게이트웨이 또는 제품의 보안 터널·자격증명 보관 기능을 별도 설계합니다.

## 8. 개별 PC 설치형 인수

이 방식은 [LOCAL_SETUP.md](LOCAL_SETUP.md)를 표준 설치서로 사용합니다.

- 기관 관리 PC와 승인된 stdio MCP 클라이언트만 대상으로 합니다.
- 모든 PC에서 동일 커밋과 Node.js LTS를 사용합니다.
- 설정 JSON에 정부 API 키가 들어가므로 파일 접근권한과 PC 반출·폐기 절차를 적용합니다.
- 시험 PC 1대에서 자동검사와 실제 질문을 확인한 후 소프트웨어 배포도구로 확장합니다.
- 업데이트·제거·키 회수까지 배포 패키지에 포함합니다.

현재 저장소는 서명된 원클릭 데스크톱 확장 파일을 제공하지 않습니다. 수동 JSON 설정이 기관 표준에 맞지
않으면 별도 패키징·서명·배포 검토가 필요합니다.

## 9. LLM 운영

지원 어댑터와 현재 코드 기본 모델은 다음과 같습니다.

| 공급자 | 키 | 기본 모델 |
|---|---|---|
| Google | `GEMINI_API_KEY` | `gemini-3.6-flash` |
| Anthropic | `ANTHROPIC_API_KEY` | `claude-haiku-4-5` |
| OpenAI | `OPENAI_API_KEY` | `gpt-5.6-luna` |

- 모델 ID와 비용은 고정 사실이 아니므로 공급자의 현행 공식 문서에서 재확인합니다.
- 여러 키를 넣지 말고 기관이 승인한 공급자를 `LLM_PROVIDER`로 명시하는 편이 감사에 유리합니다.
- 대표 질문 묶음으로 라우팅 정확도, 답변 정확도, 원문 일치, 거절·오류, 지연시간, 비용을 측정합니다.
- 키·할당량·공급자 장애 시 조회 원문으로 전환되는지 확인합니다.
- AI 요약을 공문·처분·현장 안전판단의 최종 근거로 자동 사용하지 않습니다.

## 10. 배포 승인 시험

### 자동검사

```bash
npm ci
npm run verify
npm audit
npm pack --dry-run
```

모든 명령의 날짜·Node/npm 버전·커밋 해시·결과를 변경기록에 남깁니다.

### 실제 연동 질문

| 질문 | 확인할 것 |
|---|---|
| `소방시설법 제10조` | 법령명·조 번호·본문·출처 |
| `2025년 1월 1일 화재 현황` | 날짜·관서별 필드·총 결과 |
| `아세톤 위험물이야?` | CAS·UN·위험물 품명·물성 |
| `인천 2025년 1월 교통사고 구급 통계` | 교통사고 범위 경고·`인천소방본부` 매핑 |
| `광주 2026년 8월 교통사고 구급 통계` | 통합 후 본부명이 원천 API에서 실제 조회되는지 |
| 실제 건물명과 시설 질문 | 전체 페이지 검색·건물명 필터·시설 라우팅 |
| `방화문 설치 기준` | 이름 0건 때 본문 검색과 비소방 결과 혼입 가능성 |

### 실패 시험

- 토큰 없음/오류 → 401
- 분당 한도 초과 → 429와 `Retry-After`
- 깨진 JSON → 400, 큰 chat/MCP 본문 → 413
- 정부 API 키 누락·오류·할당량 초과 → 키 값 없는 명확한 오류
- LLM 키 오류·제한시간 초과 → 조회 원문 유지
- 신뢰 프록시 설정 전/후 사용자 IP 분리 확인

## 11. 운영 점검표

### 상시·주간

- [ ] `/health` 프로세스 생존 확인
- [ ] 서버 재시작·오류율·메모리·CPU·디스크 확인
- [ ] 비정상 401/429 증가와 토큰 유출 징후 확인

`/health`는 외부 API 정상까지 검사하지 않으므로 이것만으로 전체 정상이라고 판단하지 않습니다.

### 월간

- [ ] 10장의 실제 연동 질문 재실행
- [ ] 공공 API 사용량·승인·유효기간 확인
- [ ] LLM 비용·모델 가용성·데이터 정책 확인
- [ ] 사용자·토큰·키 접근권한 검토
- [ ] 의존성 보안감사와 업데이트 후보 검토

### 변경 배포

1. 시험 환경에서 `git pull --ff-only`로 목표 커밋을 받습니다.
2. `npm ci`, `npm run verify`, 이미지 빌드, 실연동 질문을 통과합니다.
3. 문서의 버전·기능·환경변수·제한이 코드와 같은지 확인합니다.
4. 롤백할 이전 이미지/커밋과 설정 백업을 확인합니다.
5. 운영 반영 후 health와 실제 질문을 다시 확인합니다.

## 12. 장애 분류

| 증상 | 우선 확인 |
|---|---|
| 사이트가 열리지 않음 | 프로세스/컨테이너, 포트, TLS 프록시, 방화벽 |
| `/health`만 정상 | 정부 API 승인·키·트래픽, 외부망, LLM 키·할당량 |
| 401 | `CHAT_AUTH_TOKEN`/`SERVER_AUTH_TOKEN`, 앞단 Authorization 전달 |
| 전원이 429 | `TRUST_PROXY`, 프록시의 `X-Forwarded-For` 재설정 여부 |
| 특정 사용자만 429 | 해당 IP 실제 호출량과 공유 NAT 여부 |
| 법제처만 실패 | `LAW_OC`, 신청 상태, Referer/도메인/IP 조건, 기관 TLS 검사 |
| 소방청 한 기능만 실패 | 해당 API 활용승인·오퍼레이션·상세 페이지 공지 |
| 최근 통계가 없음 | 더 오래된 기준일과 원천 시스템으로 데이터 존재 여부 비교 |
| AI 요약만 실패 | LLM 공급자 상태·모델·키·할당량; 공식 조회 원문 유지 확인 |
| 건물 검색이 느림 | 시도 전체 페이지 조회 특성, API 지연·한도, 24시간 캐시 |

키가 포함될 수 있는 로그는 안전한 채널로만 전달하고 공유 전 다시 마스킹합니다.

## 13. 코드 유지보수 원칙

- 도구는 `src/tool-registry.ts` 한 곳에 등록합니다.
- 변경 의도를 먼저 실패 테스트로 만들고 수정 후 `npm run verify`를 통과합니다.
- 원천 데이터 필드 한글명·오퍼레이션은 활용가이드와 실응답으로 확인하며 추측하지 않습니다.
- 데이터베이스 추가는 호출 한도 문제가 실측되고 최신성·운영 책임을 설계한 뒤 결정합니다.
- 모델명, 가격, 승인, 호출 한도, 제품 UI를 확인 없이 고정 사실로 문서화하지 않습니다.
- 검증하지 못한 것은 [기준.md](기준.md)에 `미확인`으로 기록합니다.

## 14. 최종 인계 항목

- [ ] 승인된 GitHub 커밋/릴리스와 검증 결과
- [ ] 기관 소유 정부 API 계정 담당자와 키 보관 위치
- [ ] 사용 중인 LLM 계정·프로젝트·예산·데이터 정책
- [ ] 서버/프록시/도메인/인증/모니터링 구성도
- [ ] PC 설치형이면 표준 이미지·배포 대상·제거 절차
- [ ] 사용자 안내문과 개인정보·민감정보 입력 정책
- [ ] 장애·유출·원천 API 문의 연락망
- [ ] 다음 월간 점검일과 키/계약 갱신일
- [ ] 현재 미확인 사항과 해결 책임자

## 15. 현재 미확인 사항

- 이번 v0.6.0 변경 후 실제 정부 API 재호출: 현재 개발 환경에 키 없음
- Gemini·Claude·OpenAI 실제 API 호출: 현재 개발 환경에 키 없음
- Docker 이미지 실제 빌드/실행: 현재 개발 환경에 Docker CLI 없음
- ChatGPT/Claude 원격 MCP 연결과 단순 Bearer 인증의 종단간 호환: 해당 제품 시험 환경 없음
- 서명된 원클릭 PC 설치 패키지: 미제공

2026-08-13의 9개 도구 실호출 기록은 [기준.md](기준.md)에 보존되어 있지만 현재 버전의 재검증을 대신하지
않습니다. 위 항목을 해결하기 전에는 “기관 운영 검증 완료”라고 표시하지 않습니다.

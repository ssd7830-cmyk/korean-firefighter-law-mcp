{
  "name": "korean-firefighter-law-mcp",
  "version": "0.5.0",
  "description": "소방청 공공데이터(화재·구급·소방시설) + 법제처 소방 법령·판례를 MCP 도구로 제공. 소방관계자 실무용",
  "type": "module",
  "main": "build/index.js",
  "bin": {
    "korean-firefighter-law-mcp": "build/index.js"
  },
  "files": [
    "build"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "fast-xml-parser": "^4.5.0",
    "zod": "^3.23.0",
    "zod-to-json-schema": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
